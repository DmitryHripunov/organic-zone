// Реализовал средствами PHP
// const userName = document.querySelectorAll('.js-user-name');

// const transformUserName = value => value.trim().charAt().toUpperCase();

// userName.forEach((name) => {
//   name.textContent = transformUserName(name.textContent);
// });
